# Contributing (docs)

This page is a short pointer to the project CONTRIBUTING.md at the repository root.

See: ../CONTRIBUTING.md

Short reminders

- Do not commit movies, generated `.mivf` outputs, logs, `build/` or `dist/` folders, or temporary patch files.
- Use GitHub Releases for binary assets and attach checksums.
